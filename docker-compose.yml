services:
  frontend:
    build: ./frontend
    container_name: zoomdrive-frontend
    ports:
      - "3000:3000"

  backend:
    build: ./backend
    container_name: zoomdrive-backend
    ports:
      - "5000:5000"